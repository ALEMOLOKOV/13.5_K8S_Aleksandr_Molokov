replicaCount: 1
namespace: app1

deployment:
  name: deploy2-app1

image:
  tag: "1.20"

dataset: dataset-app1

service:
  name: helm-svc2
  type: NodePort
  ports:
    nodePort: 31200

